#include <iostream>
#include <fstream>
#include <sstream>
using namespace std;


//Escribir en un archivo
int main(){

    ofstream fout;
    char str[256];
    cout<< "Ingrese caracteres: ";
    cin.getline(str,256);

    fout.open("archiv.txt");

    fout<<str;

    fout.close();
}

//Leer por linea y por caracter
/*
int main()
{
    int numero1 = 0, numero2 = 0;
    char str[256];
    ifstream fin;

    fin.open("Archivo.txt");
    if (fin.is_open()) cout<<"Abierto"<<endl;
    else cout<<"No abierto"<<endl;


    //Para LEEER POR LINEA
    cout<<"Por Linea: "<<endl;
    while(fin.good()){
        fin.getline(str, 256);
        stringstream val(str);
        val >> numero1;
        cout<<numero1<<endl;
    }

    fin.seekg(0); //Volver al inicio del archivo

    cout<<"Por caracter: "<<endl;
    while(fin.good()){
        char temp = fin.get();
        if (temp == '\n'){
            cout<<numero2<<endl;
            numero2=0;
            }
        else if (fin.good()) numero2 = numero2*10+(temp-'0');

    }
    cout<<numero2<<endl;

    fin.close();
    return 0;
}
*/
